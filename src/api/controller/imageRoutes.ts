import { Router } from 'express';
// import ImageController from './imageController';

const router = Router();

// router.get('/imagesbybreedid', ImageController.getImagesByBreed);

export default router;
